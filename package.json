{
  "name": "afristore",
  "private": true,
  "workspaces": [
    "apps/*",
    "backend",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n backend,web -c blue,green \"npm run dev -w backend\" \"npm run dev -w apps/web\"",
    "build": "npm run build -w backend && npm run build -w apps/web",
    "lint": "npm run lint -w backend && npm run lint -w apps/web",
    "db:seed": "npm run db:seed -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
