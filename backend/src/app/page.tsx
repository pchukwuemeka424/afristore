export default function Home() {
  return (
    <main style={{ fontFamily: 'system-ui', padding: 24 }}>
      <h1>AfriStore API</h1>
      <p>
        REST endpoints live under <code>/api</code>. Use the Next.js frontend (port 3000) against this server (port
        3001).
      </p>
    </main>
  );
}
